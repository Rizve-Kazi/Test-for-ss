#include<iostream>
using namespace std;

int main()
{
    int  number_one,number_two;
    cout<<"Enter the two number = ";
    cin>>number_one>>number_two;

    cout<<noshowpoint;
    double sum = number_one + number_two;
    cout<<"\nSum = "<<sum;

    int sub = number_one - number_two;
    cout<<"\nSub = "<<sub;

    double  div = float(number_one)/number_two;
    cout<<"\nDividor = "<<div;

    int multiple = number_one*number_two;
    cout<<"\nMultiple = "<<multiple;

    int rem = number_one % number_two;
    cout<<"\nRem = " << rem;

    return 0;
}

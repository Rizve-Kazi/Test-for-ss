#include<stdio.h>
#include<string.h>
int main()
{
    char str[101];
    scanf("%d",&str);
    int len=strlen(str);

    for(int i=0;i<len;i++)
    {
        for(int j=i+1;j<len-1;j++)
        {
            if(str[i]==str[j])
            {
                for(int k=0;k<)
            }
        }
    }
}

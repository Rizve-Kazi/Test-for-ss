#include<stdio.h>
#include<math.h>
int main()
{
    int range;
    long long int number;
    scanf("%d",&range);

    for(int i=1; i<=range; i++)
    {
        scanf("%lld",&number);
        int count=0;
        int x;

       if
    }
}
